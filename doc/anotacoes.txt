____URGENTE:____
- CHANGELOG.md para o mzphp e para o mz =)

DEV:
    Variavel log = textos para a saída padrão e para o log
    Variavel dirLog = /var/log/mzphp/
    Funcao gravaLog() >> dirLog

Na instalação local:
Local da aplicacao /opt/mzphp

-Dependencias:
-- sudo
-- php7
    # apt install php
-- php-dom

-- python3
-- python3-pip
-- pip3
    # apt install python3-pip
    # pip install --upgrade pip
-- PIL, PIL.Image, PIL.ImageTK
    # apt install python-imaging python3-pil python3-pil.imagetk

-- curl

-/etc/php/7.2/apache2/php.ini
-- allow_url_fopen = On
-- display_errors = Off

-- Estrutura de pastas /var/lib
#####################atualizar
/var/lib
├── mz
│   └── desc
└── mzphp
    └── desc



